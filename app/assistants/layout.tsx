export default function AssistantsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

export const metadata = {
  title: "Assistants Performance | Solum Health QA",
  description: "Performance metrics and rankings for all assistants",
};
